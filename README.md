Tiddlywiki plugin to integrate with Vega for data visualization within wikis. Checkout the [demo](...).

To build do:

```
npm install 
cd docs
export TIDDLYWIKI_PLUGIN_PATH=../plugins/
../node_modules/.bin/tiddlywiki --build
```

